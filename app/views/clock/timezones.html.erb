<% @timezones.each do |timezone| %>
  <%= render 'timezone', timezone: timezone %>  
  
  <% if user_signed_in? %>
    <%= button_to 'Save as favorite', clock_index_path, method: :post, params: { name: timezone.name} %>
  <% end %>
  </rb>
<% end %>

<%= link_to 'Back to main', clock_index_path %>

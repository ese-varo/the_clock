<% @timezones.each do |timezone| %>
  <%= render 'shared/timezone', timezone: timezone %>  
  <%= button_to 'Save as favorite', user_clock_index_path(current_user), method: :post, params: { name: timezone.name, user_id: current_user.id} %>

  </rb>
<% end %>

<%= link_to 'Back to Home', root_path %>
